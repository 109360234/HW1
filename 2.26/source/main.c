#include <stdio.h>
#include<stdlib.h>
int main(void)
{
	int a,b;
	while (scanf_s)
	{
		printf("請輸入二個整數:\n");
		scanf_s("%d", &a);
		scanf_s("%d", &b);
		if (a % b == 0)
		{
			printf("YES %d%s", a, " is a multiple of the second\n");
		}
		else
		{
			printf("NO %d%s ", a, " is not a multiple of the second\n");
		}
	}
	system("pause");
	return 0;
}
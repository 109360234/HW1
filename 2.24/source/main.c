#include <stdio.h>
#include<stdlib.h>
int main(void)
{
	int sum;
	while (scanf_s) {
		printf("叫块计:\n");
		scanf_s("%d", &sum, "\n");
		if (sum == 1)
		{
			printf("常ぃ琌%d", sum, "\n");
		}
		else if (sum % 2 == 0)
		{
			printf("案计:%d", sum, "\n");
		}
		else if (sum % 2 != 0)
		{
			printf("计:%d", sum, "\n");
		}
	}
	system("pause");
	return 0;
}